<resources>

    <!-- Base application theme -->
    <style name="Theme.TestTask_TicketsSearch" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Base colors -->
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/blue</item>

        <!-- Theme attributes -->
        <item name="bottomSheetDialogTheme">@style/ThemeOverlay.App.BottomSheetDialog</item>

        <!-- Style attributes -->
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>
        <item name="snackbarStyle">@style/Widget.App.Snackbar</item>
        <item name="android:textViewStyle">@style/Widget.App.TextView</item>
        <item name="editTextStyle">@style/Widget.App.EditText</item>

        <!-- Params -->
        <!-- Background main color -->
        <item name="android:colorBackground">@color/black</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">?android:attr/colorBackground</item>

    </style>

</resources>